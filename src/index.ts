/**
 * The public surface of the FortressFlag Node server SDK. Everything the SDK exports is
 * enumerated here (the web SDK's index.ts rule); everything else is module-internal.
 *
 * The surface arrives with the later PRs; the scaffold ships the module so the build has an
 * entry point from day one.
 */
export const SDK_NAME = "@fortressflag/sdk-node";
